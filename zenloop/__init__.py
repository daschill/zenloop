"""Local AMD GPU/CPU stress helpers (DEPRECATED CLI).

The supported product is the C# WPF app (`ZenLoop.exe`). This package remains so
`native/build.ps1` can still drop `zenloop-hw.exe` / `zenloop-cpu.exe` into
`zenloop/bin/` for the desktop app to load.
"""

__version__ = "1.0.0"

